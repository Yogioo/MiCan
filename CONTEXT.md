# MiCan

一个网页上的无限二维画布：画布上放置节点，节点之间用边连接。

## Language

**画布**：
承载全部节点与边的无限二维平面，用户可平移、缩放查看其中任意区域。
_Avoid_: 白板、board、画板

**节点**：
画布上一个有位置的矩形元素，有种类之分。尺寸由用户设定，不随内容变化。
_Avoid_: 卡片、文本框、block

**文本节点**：
内容是一份 markdown 文本的节点，各自对应工作文件夹里的一份 md 文件。
_Avoid_: 文档节点、md 节点、笔记

**命令节点**：
内容是一条终端命令的节点，不对应任何 md 文件。它有两类接口：执行端口只接命令节点，数据端口接文本节点或命令节点。
_Avoid_: 终端节点、脚本节点、shell 节点

**缓存文件**：
命令节点最近一次运行留下的输出，一份对一个命令节点。命令节点的「值」就是它，正如文本节点的「值」是它那份 md。跟着工作文件夹走，换地方打开也在。
_Avoid_: 日志、输出文件、结果文件

**边**：
连接两个节点的有向关系，独立存在的对象，不由节点的层级隐含决定；可携带一段显示在边上的文本标签。
_Avoid_: 连线、链接、箭头

**世界坐标**：
以画布原点为基准的坐标，节点与边的位置用它表达；屏幕像素位置是它经视图变换后的结果。
_Avoid_: 全局坐标、绝对坐标

**视图**：
画布上当前可见的那块区域，由平移量与缩放比决定。
_Avoid_: 相机、viewport、镜头

**工作文件夹**：
画布状态的落地处：一个文件夹，内含画布存档、全部文本节点的 md 文件和全部命令节点的缓存文件。命令在其中执行，相对路径以它为基准。换一个地方打开它，画布就完整回来。
_Avoid_: 项目、仓库、workspace

**落盘**：
把内存里的改动写进工作文件夹的动作。改动一结束就自动发生：没有「保存」这个动作，也没有「未保存」这个状态。
_Avoid_: 保存、提交、同步、导出

**运行命令**：
在运行目录里执行命令节点的命令：先把数据边的变量替换进命令，再把这次运行的输出写进它的直接下游文本节点。一次只跑这一个节点，用来单独调试。
_Avoid_: 执行、跑脚本

**入口**：
命令节点上的一个标记，意思是「这条链从我开始」。
_Avoid_: 事件节点、开始节点、触发器

**运行链路**：
从入口出发，沿执行边把整条链跑完的动作；一次点击得到完整、可重现的结果。与「运行命令」相对：后者只跑一个节点，用来单独调试。
_Avoid_: 全跑、执行流程

**运行目录**：
命令节点的命令实际执行的那个文件夹。默认是工作文件夹；可以在工具条上给全局定一个本机绝对路径，单个节点自己再设一个就覆盖全局。
节点自己那个写成相对工作文件夹的路径（`.` 就是工作文件夹本身），存档才能换个地方打开；写绝对路径也行，那是这台机器上的一份。
全局那个存在用户目录里（跟这台机器走），不进画布存档。
_Avoid_: cwd、工作目录、当前目录

**执行边**：
连接两个命令节点的边，只表达先后：它指出的那个命令节点在来路那个跑完之后跑。不携带任何数据。
一个命令节点的执行输入只收一条执行边 —— 链是直线，不会合流。
_Avoid_: 事件线、流程线、控制线

**数据边**：
把一份文本从一处送到另一处的边，两端都可以是命令节点或文本节点：来路是文本节点时，送的是它的正文；来路是命令节点时，送的是它这次运行的输出。去处是命令节点时进它的变量池，去处是文本节点时写进那份文档。
标签就是变量名；运行命令前替换掉命令里的 `{{变量名}}`。值必须是单行。
来路是命令节点时，送的是它**最近一次运行**的输出（在链里，上游刚跑完，所以就是这一次；单独跑下游时，就是上一次那个）。上游从来没跑过就没有值，此时单独运行下游会报错，让你先跑它或改跑整条链。
同一个名字有两种取法：`{{变量名}}` 取正文，`[[变量名]]` 取那份 md 文件的本机绝对路径（干活的是运行目录，多半不在工作文件夹里，所以给绝对的）。`[[ ]]` 里带空白的不算 token，原样交给 shell。
_Avoid_: 注入边、参数线、输入线
