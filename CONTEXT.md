# MiCan

一个网页上的无限二维画布：画布上放置节点，节点之间用边连接。

## Language

**画布**：
承载全部节点与边的无限二维平面，用户可平移、缩放查看其中任意区域。
_Avoid_: 白板、board、画板

**节点**：
画布上一个有位置的矩形元素，有种类之分。尺寸由用户设定，不随内容变化。
_Avoid_: 卡片、文本框、block

**文本节点**：
内容是一份 markdown 文本的节点，各自对应工作文件夹里的一份 md 文件。
_Avoid_: 文档节点、md 节点、笔记

**命令节点**：
内容是一条终端命令的节点，不对应任何 md 文件。它有两类接口：执行端口只接会跑的节点，数据端口接文本节点或会跑的节点。
_Avoid_: 终端节点、脚本节点、shell 节点

**提取节点**：
从一段文本里取出一个单行字符串的节点，取出来的那个字符串就是它的值。取法写在节点上，源默认是执行来路那个节点的值。
取不到就停并报错，不静默给空值。
_Avoid_: 条件节点、判定节点、JSON 节点、解析节点

**会跑的节点**：
命令节点与提取节点合称：都有值、都有缓存文件、都能进链。文本节点不是。
_Avoid_: 执行节点、任务节点

**取法**：
提取节点上写的一段文本，说明从源里取什么：`格式:路径`，如 `json:isFull`、`json:a.b`；不写格式就是 json。
_Avoid_: 表达式、规则、正则

**缓存文件**：
会跑的节点最近一次运行留下的输出，一份对一个节点。它的「值」就是它，正如文本节点的「值」是它那份 md。跟着工作文件夹走，换地方打开也在。
_Avoid_: 日志、输出文件、结果文件

**边**：
连接两个节点的有向关系，独立存在的对象，不由节点的层级隐含决定。可以带一段写在边上的标签，两族边各有各的用法：数据边上是变量名，执行边上是期望匹配的值。
_Avoid_: 连线、链接、箭头

**世界坐标**：
以画布原点为基准的坐标，节点与边的位置用它表达；屏幕像素位置是它经视图变换后的结果。
_Avoid_: 全局坐标、绝对坐标

**视图**：
画布上当前可见的那块区域，由平移量与缩放比决定。
_Avoid_: 相机、viewport、镜头

**选中**：
当前被点中或框住的那些节点与边的集合，空集合就是没选。框选时节点比矩形、边比它画出来的那根曲线，
搭上一点就算选中（不要求整条都在框里）。它只是界面上的临时状态：不进存档，也不进撤销栈。
Delete 一次删掉整个选中；选中之后拖其中一个，整批一起走。
_Avoid_: 高亮、激活、焦点

**工作文件夹**：
画布状态的落地处：一个文件夹，内含画布存档、全部文本节点的 md 文件和全部命令节点的缓存文件。命令在其中执行，相对路径以它为基准。换一个地方打开它，画布就完整回来。
_Avoid_: 项目、仓库、workspace

**落盘**：
把内存里的改动写进工作文件夹的动作。改动一结束就自动发生：没有「保存」这个动作，也没有「未保存」这个状态。
_Avoid_: 保存、提交、同步、导出

**运行命令**：
在运行目录里跑一个节点：先把数据边的变量替换进命令，再把这次运行的值写进它的直接下游文本节点。一次只跑这一个，用来单独调试。
提取节点上叫「运行提取」，是同一件事。
_Avoid_: 执行、跑脚本

**触发节点**：
入口节点与定时器节点合称：只有执行出边，没有值、没有缓存文件，也不传数据。会跑的节点的反面。
_Avoid_: 事件节点、开始节点

**入口节点**：
一枚独立节点，意思是「这条链从我开始」：一根执行出边，指向第一个会跑的节点。将来子图的入口就是它。
_Avoid_: 开始节点、事件节点

**定时器节点**：
触发节点的一种：节点上就是一组控件（模式、数值、时刻），写出一行时间表，一根执行出边指向第一个会跑的节点；到点由后端从那儿跑完整条链。
一条链还在跑时，这一圈跳过。
_Avoid_: 时钟节点、计划节点

**时间表**：
定时器节点上那行文本，说什么时候响：`每 30 秒` / `每 30 分钟` / `每天 09:30`。
它是节点上那组控件写出来的，也是调度器读的唯一一份；两边都不另存一份。
_Avoid_: 表达式、cron、计划

**运行链路**：
从触发节点（入口或定时器）出发的一次运行：当前节点跑完，拿它的值跟各条执行出边的标签比对，顺着匹配的那根走下去，直到没有边可走。
一条链的身份看链身从哪个会跑的节点起步，不看是谁点的火（人手从入口进去与定时器进去是同一条链）。与「运行命令」相对：后者只跑一个节点，用来单独调试。
_Avoid_: 全跑、执行流程

**一轮**：
环上从某个节点走回它自己的那一段。只在界面上用来计数，不影响怎么走。
_Avoid_: 圈、遍、迭代

**设置**：
分两层：跟这台机器走的「全局配置」，和跟这份画布走的「存档配置」。两层在同一个设置窗口里改。
_Avoid_: 偏好、config

**全局配置**：
跟这台机器走、不进存档的那些值：命令行、超时、输出上限、启动打开哪个文件夹、界面手感。存在用户目录里，换工作文件夹不变。
_Avoid_: 机器配置、本地设置

**存档配置**：
跟这份画布走、进存档的那些值：运行目录、跑链路最多走多少步。换个工作文件夹打开就跟着变。
_Avoid_: 画布设置、工作区配置

**运行目录**：
命令节点的命令实际执行的那个文件夹。默认是工作文件夹；存档里可以给整份画布定一个，单个节点自己再设一个就覆盖它。
两层都写在存档里（画布那份在存档顶层、节点那份在节点上），都写成相对工作文件夹的路径（`.` 就是工作文件夹本身），也可以写本机绝对路径。
_Avoid_: cwd、工作目录、当前目录

**命令行**：
命令交给哪个命令行去跑（cmd / PowerShell / bash…）。它是全局配置：存在用户目录里，空着就用这台机器的默认（Windows 上是 cmd）。
_Avoid_: shell、外壳、终端类型、解释器

**执行边**：
从触发节点或会跑的节点出发、表达先后与分路：标签是期望匹配的值，空标签是兜底（没有别的出边匹配时走它）。它自己不携带数据 —— 比的是这根边来路那个节点自己的值。
触发节点各自只带一根出边，都指向会跑的节点（定时器→第一个会跑的节点、入口也是）；会跑的节点的出边不限根数，也可以回头指向已经走过的节点，于是成环。入边也不限根数：走路时光标只有一个，「从哪儿来」永远是确定的。
_Avoid_: 事件线、流程线、控制线

**扩展**：
工作文件夹里 `extensions/` 底下的一个目录，里面是一份 `EXTENSION.md`（声明）和它自己的实现；一个目录就是一个动作。
节点引用它，于是同一段实现能在画布上被拖出很多份 —— 这是 MiCan 里「复用」的唯一单位。
含 `EXTENSION.md` 的目录就是扩展，是叶子，不再往下扫；不含的目录只是菜单里的分组，继续往下，目录层级就是菜单层级。
这个名字跟存档里的 `nodes`（画布上的节点）不共用：一个在盘上，一个在画布上。
_Avoid_: 插件、节点模板、脚本

**脚本**：
扩展里那份真正跑起来的实现，一份对一个动作：包住一次外部调用（`gh`、`tapd`、`pi`、`codex`…），把各家 CLI 的差别收在自己身上。
它在扩展目录里，由那份 `EXTENSION.md` 的 `entry` 指到；调用它的是画布，不是用户。
对上只认一套进出：进的是提示词、技能、工具、模型这些，出的是**一段 JSON**。JSON 不合格由脚本自己兜成一份「失败」的 JSON，不让它漏到链上。
业务编排不写在脚本里 —— 挑工单、实现、验收这些先后关系在画布上。
_Avoid_: 技能、插件、扩展

**数据边**：
把一份文本从一处送到另一处的边，两端都可以是命令节点或文本节点（触发节点不在此列，它们不传值）：来路是文本节点时，送的是它的正文；来路是命令节点时，送的是它这次运行的输出。去处是命令节点时进它的变量池，去处是文本节点时写进那份文档。
标签就是变量名；运行命令前替换掉命令里的 `{{变量名}}`。值必须是单行。
来路是命令节点时，送的是它**最近一次运行**的输出（在链里，上游刚跑完，所以就是这一次；单独跑下游时，就是上一次那个）。上游从来没跑过就没有值，此时单独运行下游会报错，让你先跑它或改跑整条链。
同一个名字有两种写法：`{{变量名}}` 取正文，`[[变量名]]` 取那份 md 文件的本机绝对路径（干活的是运行目录，多半不在工作文件夹里，所以给绝对的）。`[[ ]]` 里带空白的不算 token，原样交给 shell。
_Avoid_: 注入边、参数线、输入线

**输入**：
命令里 `{{名字}}` / `[[名字]]` 带出来的东西；扩展节点则是那份清单的 `args` 带出来的。
节点左边一行一个：端口接着边，旁边的框填常量 —— 两条路给同一个名字，连了边就以边为准。
扩展能在清单里给输入写**默认值**：留空又没连线的输入拿它顶上，所以拖出来的节点上那些框是空的。
没有另外一份输入声明，输入名就是命令里那几个 token。
_Avoid_: 参数、变量表、引脚
