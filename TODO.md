- [ ] 入口或定时器上应该有配置, 如果相连的节点正在运行, 是否应该可以激活. 因为激活可能会导致正在进行的任务重新开始, 比如 pi 拓展节点中可能正在进行任务, 这个时候定时器触发会导致任务直接强制中断, 所以这个需要配置项. 可以默认为如果相连的链路中,有运行节点在运行的话,就不应该从入口触发整个链路
- [ ] 认为触发进化中, 我可能需要持续对话, 用于补充细节, 交互式的对话方式. 目前的方式挺好, 但是每次有个小改动, 可能就需要完整的跑一边进化流程了. 可能需要聊聊这个点
