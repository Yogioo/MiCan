- [x] 能网页渲染的的2D无限画布
- [x] 需要文本节点支持markdown文本渲染(不要求复杂Latex语法)
- [ ] 导出改为保存和另存为，增加打开命令
- [ ] 文本节点需要支持文件名设置，新增后，应直接生成对应md文件，保存文本节点修改应直接覆写目标文件，json中文本节点应配置相对路径，指向目标文件，存储文件结构为 工作文件夹，内部是 json + docs文件夹，docs文件夹里面有所有md文件。  
- [ ] 新增终端命令节点，节点内可配置命令信息，比如可配置 "ls" 命令， 选中右键可打开菜单/运行命令， 即可在当前目录运行 ls 命令，并返回给连线的下级文件，覆盖并写入目标md文件
